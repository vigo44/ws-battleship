export type dataInputRegType = {
  name: string;
  password: string;
};

export type dataOutputRegType = {
  name: string;
  index: string | number;
  error: boolean;
  errorText: string;
};
